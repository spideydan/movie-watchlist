<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <h1>Movies</h1>

    <form action="/movies/findMovie" method='POST'>
        <input type="text" id="search" class="search" placeholder="Enter a movie" name='movieItem'>
        <input type="submit">
    </form>

    <!-- <h2>
        <%= user.userName %> has <%= left %> things left to do.
    </h2> -->
    
    <ul>
        <% movies.forEach( el=> { %>
            <li class='movieItem' data-id='<%=el._id%>' data-title =>
                <span class='<%= el.completed === true ? ' completed' : 'not' %>'><%= el.movie %></span>
                <span class='del'> Delete </span>
            </li>
            <% }) %>
    </ul>

    <a href="/logout">Logout</a>

    <script src="/js/main.js"></script>
</body>

</html>